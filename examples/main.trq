use std::io::Input
use std::io::Print

const main = "idk"	
fn asd (i: bool): bool { // no async
	let var = Input()
}
fn async main (i: bool) { // async is there hence there will be a isasync = true
	let var = Input()
	let asd = asd()
}